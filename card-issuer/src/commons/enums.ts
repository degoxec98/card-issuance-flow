export enum ProductCurrency {
  PEN = "PEN",
  USD = "USD",
}

export enum DocumentType {
  DNI = "DNI",
}

export enum ProductType {
  VISA = "VISA",
}

export enum CardStatus {
  PENDING = "PENDING",
  EMITTED = "EMITTED",
}